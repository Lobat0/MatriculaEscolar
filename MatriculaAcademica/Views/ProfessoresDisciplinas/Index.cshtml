@model IEnumerable<MatriculaAcademica.Models.ProfessorDisciplina>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Disciplina.nome_disciplina)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Professor.nome_professor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Disciplina.nome_disciplina)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Professor.nome_professor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_professor_disciplina }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_professor_disciplina }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_professor_disciplina })
        </td>
    </tr>
}

</table>
